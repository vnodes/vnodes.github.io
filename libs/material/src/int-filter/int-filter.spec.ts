import { IntFilter } from './int-filter';

describe('IntFilter', () => {
  it('should create an instance', () => {
    const directive = new IntFilter();
    expect(directive).toBeTruthy();
  });
});
